# Домашнее задание 12.2

## Описание:

Домашнее задание включает в себя работу с CSV и XLSX файлами, а так же использование Pandas.

## Этапы работы:

 1. Создание ветки homework_13_1.
 2. Работа с библиотекой Pandas, создание новых модулей, функций и тестов к ним.
 3. Проверка кода с помощью линтеров и исправление ошибок.
 4. Дополнение информации в README.md
 5. Сдача домашнего задания через Pull Request.

## Использование:

В проекте реализованы **13 функции** которые позволяют проводить следующие операции:

### 1. Маскировка счёта или карты.
```
Функция mask_account_card принимает счёт или карту и шифрует в
зависимости от содержимого.
Если введена карта - то выводится полный номер карты с 6
зашифрованными цифрами.
Например: 1234 12** **** 1234

Если введён счёт, то выводится последние 4 цифры и две цифры
которые были зашифрованы.
```

### 2. Вывод даты в читаемом формате.
```
Функция get_date принимает строку с датой и возвращает часть
содержимого в нужной последовательности.
```

### 3. Сортировка по значению ключа state.
```
Функция filter_by_state принимает список словарей и возвращает новый список
со словарями у которых ключ state равен значению EXECUTED
```

### 4. Сортировка по дате.
```
Функция sort_by_date сортирует принимаемый список словарей и
возвращает отсортированный в порядке убывания.
```

### 5. Маскировка номера счёта.
```
Функция get_mask_account это первая версия функции mask_account_card.
Является отдельной и самостоятельной функцией которая выводит 4
последние цифры карты и шифрует две предыдущие в формате "XX1234".
```

### 6. Маскировка номера карты.
```
Функция get_mask_card_number это первая версия функции
mask_account_card. Является самостоятельной функцией которая позволяет
шифровать часть номера карты в формате "1234 12** **** 1234".
```

### 7. Фильтрация по валюте.
```
Генератор filter_by_currency принимает список словарей транзакций
и название валюты. Возвращает транзакцию из списка с искомой валютой.
```

### 8. Описание транзакций
```
Генератор transaction_descriptions принимает список словарей и проводит
итерацию по словарям. Возвращает описание транзакции.
```

### 9. Генерация номера карты.
```
Генератор card_number_generator генерирует номер карты в нужном
диапазоне и приводит в корректный формат.
```


### 10. Чтение файлов с транзакциями.
```
Функция read_file принимает на вход название файла и возвращает список
словарей с транзакциями.
```

### 11.  Конвертация валюты.
```
Функция convert_amount принимает транзакцию и возвращает перевод в рубли.
```

### 12.  Чтение CSV файлов.
```
Функция read_scv_transaction читает транзакции из CSV-файла и возвращает
их в виде списка словарей.
```

### 13.  Чтение XLSX (Excel) файлов.
```
Функция read_excel_transactions читает транзакции из XLSX-файла и возвращает
их в виде списка словарей
```

## Тестирование.

### Краткое описание


✅ **В проекте реализованы тесты вышеперечисленных функций.**

В них проверяется базовая функциональность функций и генераторов, а так же некорректные форматы и остальные вводимые
данные. Каждая функция проверена на работоспособность и получение ожидаемых результатов и ошибок.

В тестах используются как **фикстуры** так и **параметризация**, для более широкого охвата некорректных и корректных вводимых
данных.

Создан декоратор, который помогает "оборачивать" функции для отслеживания логов и записи их в filename (любое название файла) либо консоль.

Любые действия в модулях utils и masks записываются в логи, для отслеживания корректной работы программы.

### Masks

```Созданные тесты:
• Проверка функциональности get_mask_card_number.
• Проверка некорректных значений get_mask_card_number.
• Проверка длины номера карты в изначальном варианте и корректном формате.
• Проверка функциональности get_mask_account.
• Проверка некорректных значений в get_mask_account.
• Проверка длины замаскированных карт.
```

### Processing

```
• Проверка работы функции filter_by_state.
• Проверка некорректных значений функции filter_by_state.
• Проверка корректных значений state.
• Проверка функции sort_by_date.
```

### Widget

```
• Проверка работы функции mask_account_card.
• Проверка ошибочных аргумнентов в masc_account_card.
• Проверка работы функции get_date.
• Проверка ошибочных аргументов функции get_date.
```

### Generators

```
• Проверка поиска допустимых валют в списке.
• Проверка списка с несуществующей валютой.
• Проверка пустого списка с существующей валютой.
• Проверка извлечения описаний.
• Проверка работы с пустым списком.
• Проверка формата, длины номера карты.
• Проверка ожидаемой генерации с использованием параметризации.
• Проверка корректной генерации чисел в заданном диапазоне.
```

### Decorators

```
• Проверка логирования в файл при успешном выполнении функции.
• Проверка вывода лога в консоль при отсутствии файла.
• Проверка логирования ошибок в файл при делении на ноль.
• Проверка сохранения метаданных декорируемой функции.
```

### Utils

```
• Тест корректного чтения валидного JSON-списка.
• Тест чтения JSON, который не является списком.
• Тест возвращает пустой список при отсутствии файла.
• Тест поведения при отсутствии имени файла.
```

### External_api

```
• Тест с конвертацией RUB в RUB
• Тест суммы равное нулю (граничное значение)
• Тест обработки HTTP-ошибки
• Тест обработки ошибки при конвертации
```

### Transactions_read

```
• Тест успешного прочтения CSV файла.
• Тест обработки отсутствующего CSV файла.
• Тест обработки ошибки чтения CSV файла
• Тест успешного чтения XLSX файла
• Тест обработки ошибки XLSX файла
```